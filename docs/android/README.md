# Android UI screenshots

Captured on Samsung SM-S9280 (Android 16) for PR review.

| File | Description |
|------|-------------|
| `ui-main.png` | Main Compose shell (OLED dark), proxy stopped |

Prefer linking from the PR body so reviewers see the UI without installing.
