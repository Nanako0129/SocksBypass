<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- OLED dark shell; status/nav bars drawn edge-to-edge by Compose. -->
    <style name="Theme.SocksBypass" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">#020617</item>
        <item name="android:colorBackground">#020617</item>
    </style>
</resources>
